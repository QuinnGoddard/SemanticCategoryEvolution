let appearedWords = new Set()
const wordList = ["fer", "yaum", "xew", "pior", "trif", "drog"]


// clicking on word adds properties of image to word
function handleWordClick(event) {
  // add word to list of words that have appeared
  appearedWord.add(event.target.value)
  
}

// each image associated with list of properties
const images = {
  A: {num: 1, color: blue, spots: yes, shape: square, file: ...},
  B:
  C:
  D: 
}

